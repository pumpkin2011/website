<%= form_for(@m_role) do |f| %>
  <% if @m_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@m_role.errors.count, "error") %> prohibited this m_role from being saved:</h2>

      <ul>
      <% @m_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="panel panel-default">
  <div class="panel-heading"><h3><%= @m_role.persisted? ? "Editing M_Role" : "New M_Role" %></h3></div>
  <div class="panel-body">

    <div class="field">
      <%= f.label :role_no %><br>
      <%= f.text_field :role_no, class: "text_style" %>
    </div>
    <div class="field">
      <%= f.label :role %><br>
      <%= f.text_field :role, class: "text_style" %>
    </div>

    <br />

    <div class="actions">
      <%= f.submit class: "btn btn-primary" %>
      <%#= link_to 'Show', @m_role %> &nbsp;
      <%= link_to 'Back', m_roles_path, class: "btn btn-default" %>
    </div>

  </div>
  </div>

<% end %>
