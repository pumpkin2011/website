<%= form_for(@console_desk) do |f| %>
  <% if @console_desk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@console_desk.errors.count, "error") %> prohibited this console_desk from being saved:</h2>

      <ul>
      <% @console_desk.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
